## Build Setup

``` bash
# first of all you need to install  project dependencies
npm install

# project will run at port 8080 (if 8080 is in use, 8081 will be used to run project)
npm start

# use command bellow to run unit test
npm test
```
